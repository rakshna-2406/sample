npx create-react-app sample
cd sample
npm start

npm install firebase
npm install -g firebase-tools
firebase --version
firebase login
firebase init hosting
npm run build
firebase deploy

App.js
import React, { useState } from "react";
import { auth } from "./firebaseConfig";
import { createUserWithEmailAndPassword, signInWithEmailAndPassword } from "firebase/auth";
import { uploadFile } from "./dropboxService";

function App() {
  const [isSignup, setIsSignup] = useState(false);
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [file, setFile] = useState(null);

  const handleSubmit = async (e) => {
    e.preventDefault();
    try {
      if (isSignup) {
        await createUserWithEmailAndPassword(auth, email, password);
        alert("Signed Up Successfully!");
      } else {
        await signInWithEmailAndPassword(auth, email, password);
        alert("Logged In Successfully!");
      }
    } catch (error) {
      alert("Error: " + error.message);
    }
  };

  const handleUpload = () => {
    if(!file){
      alert("Please select a file");
      return;
    }
    uploadFile(file).then((res) => {
      if(res.path_display){
        alert("Uploaded:" + res.path_display);
      }
      else{
        alert("Upload failed");
      }
    });
  };

  return (
    <div style={styles.container}>
      <h2>{isSignup ? "Sign Up" : "Login"}</h2>

      <form onSubmit={handleSubmit} style={styles.form}>
        <input type="email" placeholder="Email" value={email} onChange={(e) => setEmail(e.target.value)} style={styles.input} required />
        <input type="password" placeholder="Password" value={password} onChange={(e) => setPassword(e.target.value)} style={styles.input} required />
        <button type="submit" style={styles.button}>{isSignup ? "Sign Up" : "Login"}</button>
      </form>

      <div style={{ marginTop: "20px" }}>
        <h3>Dropbox Upload</h3>
        <input type="file" onChange={(e) => setFile(e.target.files[0])} style={styles.input} />
        <button onClick={handleUpload} style={styles.button}>Upload to Dropbox</button>
      </div>

      <p style={{ cursor: "pointer", marginTop: "20px" }} onClick={() => setIsSignup(!isSignup)}>
        {isSignup ? "Already have an account? Login" : "Don't have an account? Sign Up"}
      </p>
    </div>
  );
}

const styles = {
  container: { display: "flex", flexDirection: "column", alignItems: "center", fontFamily: "Arial", marginTop: "100px" },
  form: { display: "flex", flexDirection: "column", width: "300px" },
  input: { padding: "8px", margin: "5px 0", fontSize: "16px" },
  button: { padding: "10px", marginTop: "10px", backgroundColor: "blue", color: "white", fontSize: "16px", cursor: "pointer" }
};

export default App;

firebaseConfig.js:
// src/firebaseConfig.js
import { initializeApp } from "firebase/app";
import { getAuth } from "firebase/auth";

const firebaseConfig = {
  apiKey: "AIzaSyCdiObMMzXQ8fbx4FYHZF0CKO1nu2gfM6Y",
  authDomain: "simple-auth-ui.firebaseapp.com",
  projectId: "simple-auth-ui",
  storageBucket: "simple-auth-ui.firebasestorage.app",
  messagingSenderId: "502327975815",
  appId: "1:502327975815:web:9f059e7907e48dc1653d22",
  measurementId: "G-YQSD15J95J"
};

const app = initializeApp(firebaseConfig);
export const auth = getAuth(app);

dropboxService.js
const TOKEN = "";
export const uploadFile = (file) =>
  fetch("https://content.dropboxapi.com/2/files/upload", {
    method: "POST",
    headers: {
      Authorization: `Bearer ${TOKEN}`,
      "Content-Type": "application/octet-stream",
      "Dropbox-API-Arg": JSON.stringify({ path: "/" + file.name, mode: "add" }),
    },
    body: file,
  }).then((res) => res.json());

Postman
node --version
npm --version
npm init -y
npm install express
npm install nodemon
node server.js

const express = require('express');
const app = express();
app.use(express.json());

let books=[{id:1,title:'Harry Potter'},{id:2,title:'The 5AM Clue'}];

app.get('/api/books',(req,res)=>res.send(books));

app.post('/api/books',(req,res)=>{
  books.push({id:books.length+1, title:req.body.title});
  res.send('Book Added');
});

app.put('/api/books/:id',(req,res)=>{
  const b=books.find(x=>x.id==req.params.id);
  if(!b) return res.send('Not Found');
  b.title=req.body.title;
  res.send('Book Updated');
});

app.delete('/api/books/:id',(req,res)=>{
  books=books.filter(x=>x.id!=req.params.id);
  res.send('Book Deleted');
});

app.listen(3000,()=>console.log('Server running on http://localhost:3000/api/books'));

App.js(Auth0):
npm install @auth0/auth0-react
import React from "react";
import { useAuth0 } from "@auth0/auth0-react";
function App() {
  const { loginWithRedirect, logout, isAuthenticated, user } = useAuth0();

  return (
    <div style={styles.container}>
      {!isAuthenticated ? (
        <>
          <h2>Login / Sign Up</h2>
          <button style={styles.button} onClick={() => loginWithRedirect()}> Log In / Sign Up </button>
        </>
      ) : (
        <>
          <h2>Welcome, {user.name || user.email}!</h2>
          <button style={styles.button} onClick={() => logout({ logoutParams: { returnTo: window.location.origin } })}> Log Out </button>
        </>
      )}
    </div>
  );
}

const styles = {
  container: {display: "flex",flexDirection: "column",alignItems: "center",justifyContent: "center",fontFamily: "Arial",height: "100vh", background: "linear-gradient(135deg, #667eea, #764ba2)", color: "white",},
  button: {padding: "10px 20px",marginTop: "10px",backgroundColor: "#ff7e5f", color: "white",fontSize: "16px",cursor: "pointer",border: "none",borderRadius: "5px",},
};

export default App;


index.js
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import {Auth0Provider} from "@auth0/auth0-react";

const domain ="dev-yx0yow8h13o1rxdj.us.auth0.com";
const clientId = "PwG61Ef6GyaRfoTUvqzb8QDPl2l48ALj";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <Auth0Provider
      domain = {domain}
      clientId = {clientId}
      authorizationParams={{redirect_uri : window.location.origin}}
  >
    <App />
  </Auth0Provider>
);

server.js(mongodb Atlas);
npm init -y
npm install express
npm install mongoose
node server.js

const express = require('express');
const mongoose = require('mongoose');
const app = express();

app.use(express.json());

// Connect to MongoDB Atlas
mongoose.connect('mongodb+srv://rakshnata_db_user:0giIiQcdOpYagL6o@cluster0.gragc5l.mongodb.net/bookDB', {
    useNewUrlParser: true,
    useUnifiedTopology: true
})
.then(() => console.log("Connected to MongoDB Atlas"))
.catch(err => console.error(" MongoDB connection error:", err));

// Schema and model
const Book = mongoose.model('Book', new mongoose.Schema({
    title: String
}));

// Sample data in code
const initialBooks = [
    { title: "Harry Potter" },
    { title: "The Hobbit" },
    { title: "Ponniyin Selvan" }
];

// Function to insert data automatically if not already in DB
async function initializeData() {
    const count = await Book.countDocuments();
    if (count === 0) {
        await Book.insertMany(initialBooks);
        console.log(" Initial data added to MongoDB Atlas");
    } else {
        console.log(" Data already exists in MongoDB Atlas");
    }
}

// Call it after connection
mongoose.connection.once('open', initializeData);

// GET all books
app.get('/books', async (req, res) => {
    const books = await Book.find();
    res.json(books);
});

// ADD a book
app.post('/books', async (req, res) => {
    await Book.create({ title: req.body.title });
    res.send(' Book added to MongoDB Atlas');
});

// UPDATE a book
app.put('/books/:id', async (req, res) => {
    await Book.findByIdAndUpdate(req.params.id, { title: req.body.title });
    res.send(' Book updated in MongoDB Atlas');
});

// DELETE a book
app.delete('/books/:id', async (req, res) => {
    await Book.findByIdAndDelete(req.params.id);
    res.send(' Book deleted from MongoDB Atlas');
});

// Start the server
app.listen(3000, () => {
    console.log(' Server running at http://localhost:3000/books');
});

MPI:
sudo apt update
sudo apt install -y openmpi-bin libopenmpi-dev
sudo apt install -y python3-pip
pip3 install mpi4py
mpirun --version
mpicc --version
python3 -c "import mpi4py; print(mpi4py.__version__)"

hello.py
from mpi4py import MPI
def main():
    comm = MPI.COMM_WORLD      
    rank = comm.Get_rank()
    size = comm.Get_size()
    print(f"Hello from process {rank} out of {size}")

if __name__ == "__main__":     
    main()
mpirun -np 4 python3 hello.py
mpirun --oversubscribe -np 4 python3 hello.py

OpenMP
sudo apt update
sudo apt install -y python3-pip
pip3 install numba numpy

hello_openmp.py

from numba import njit, prange
@njit(parallel=True)
def hello_openmp(n):
    for i in prange(n):
        print(f"Hello from thread {i}")

def main():
    threads = 4   
    hello_openmp(threads)

if __name__ == "__main__":
    main()

python3 hello_openmp.py

Spark:
sudo apt update
sudo apt install openjdk-17-jdk -y
sudo apt-get install scala
go to firefox-> spark-hadoop download
star symbol-> 3rd link
wget https://dlcdn.apache.org/spark/spark-4.0.1/spark-4.0.1-bin-hadoop3.tgz
tar -xzf spark-4.0.1-bin-hadoop3.tgz
sudo mv spark-4.0.1-bin-hadoop3 spark
sudo mv spark /usr/local/spark
gedit ~.bashrc
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
export HADOOP_HOME=/home/osboxes/hadoop-3.3.6
export HADOOP_INSTALL=$HADOOP_HOME
export HADOOP_MAPRED_HOME=$HADOOP_HOME
export HADOOP_COMMON_HOME=$HADOOP_HOME
export HADOOP_HDFS_HOME=$HADOOP_HOME
export HADOOP_YARN_HOME=$HADOOP_HOME
export SPARK_HOME=/usr/local/spark
export PATH=$PATH:$SPARK_HOME/bin

source ~./bashrc
spark-shell
// 1. Load your input file into an RDD
val inputRDD = sc.textFile("/home/osboxes/input.txt")

// 2. Print the first 5 lines to check
inputRDD.take(5).foreach(println)

// 3. Split each line into words
val wordsRDD = inputRDD.flatMap(line => line.split(" "))

// 4. Map each word to (word, 1)
val wordPairs = wordsRDD.map(word => (word, 1))

// 5. Count occurrences of each word
val wordCounts = wordPairs.reduceByKey(_ + _)

// 6. Collect and print the results
wordCounts.collect().foreach(println)

:quit

Hadoop:

sudo apt update
sudo apt install openjdk-17-jdk ssh rsync -y
java -version
cd ~
wget https://downloads.apache.org/hadoop/common/hadoop-3.3.6/hadoop-3.3.6.tar.gz
tar -xzf hadoop-3.3.6.tar.gz
mv hadoop-3.3.6 hadoop
dirname $(dirname $(readlink -f $(which java)))
gedit ~/hadoop/etc/hadoop/hadoop-env.sh
export JAVA_HOME=/usr/lib/jvm/java-17-openjdk-amd64
gedit ~/.bashrc
export JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64
export HADOOP_HOME=/home/$USER/hadoop
export HADOOP_INSTALL=$HADOOP_HOME
export HADOOP_MAPRED_HOME=$HADOOP_HOME
export HADOOP_COMMON_HOME=$HADOOP_HOME
export HADOOP_HDFS_HOME=$HADOOP_HOME
export HADOOP_YARN_HOME=$HADOOP_HOME
export HADOOP_COMMON_LIB_NATIVE_DIR=$HADOOP_HOME/lib/native
export PATH=$PATH:$HADOOP_HOME/sbin:$HADOOP_HOME/bin
export HADOOP_OPTS="-Djava.library.path=$HADOOP_HOME/lib/native"
source ~/.bashrc
cd ~/hadoop/etc/hadoop
gedit core-site.xml
<configuration>
    <property>
        <name>fs.defaultFS</name>
        <value>hdfs://localhost:9000</value>
    </property>
</configuration>
gedit hdfs-site.xml
<configuration>
    <property>
        <name>dfs.replication</name>
        <value>1</value>
    </property>
    <property>
        <name>dfs.name.dir</name>
        <value>/home/osboxes/hadoopdata/namenode</value>
    </property>
    <property>
        <name>dfs.data.dir</name>
        <value>/home/osboxes/hadoopdata/datanode</value>
    </property>
</configuration>
cp mapred-site.xml.template mapred-site.xml
gedit mapred-site.xml
<configuration>
    <property>
        <name>mapreduce.framework.name</name>
        <value>yarn</value>
    </property>
</configuration>
gedit yarn-site.xml
<configuration>
    <property>
        <name>yarn.nodemanager.aux-services</name>
        <value>mapreduce_shuffle</value>
    </property>
    <property>
        <name>yarn.nodemanager.auxservices.mapreduce.shuffle.class</name>
        <value>org.apache.hadoop.mapred.ShuffleHandler</value>
    </property>
</configuration>
ssh-keygen -t rsa -P ""
cat ~/.ssh/id_rsa.pub >> ~/.ssh/authorized_keys
chmod 640 ~/.ssh/authorized_keys
ssh localhost
hdfs namenode -format
start-dfs.sh
start-yarn.sh
jps
NameNode
DataNode
NodeManager
ResourceManager
SecondaryNameNode
HDFS NameNode → http://localhost:9870

YARN ResourceManager → http://localhost:8088

hdfs dfs -mkdir /input
echo "Hadoop MapReduce word count example Hadoop Hadoop" > input.txt
hdfs dfs -put input.txt /input

hadoop jar $HADOOP_HOME/share/hadoop/mapreduce/hadoop-mapreduce-examples-3.3.6.jar wordcount /input /output

hdfs dfs -cat /output/part-r-00000

Hadoop 3
MapReduce 1
word 1
count 1
example 1

mkdir -p /home/osboxes/hadoopdata/namenode
mkdir -p /home/osboxes/hadoopdata/datanode

/home/osboxes/hadoop/bin/hdfs namenode -format

/home/osboxes/hadoop/sbin/stop-dfs.sh
/home/osboxes/hadoop/sbin/stop-yarn.sh
/home/osboxes/hadoop/sbin/start-dfs.sh
/home/osboxes/hadoop/sbin/start-yarn.sh

cd ~
mkdir wordcount
cd wordcount

gedit WordCount.java

import java.io.IOException;
import java.util.StringTokenizer;
import org.apache.hadoop.conf.Configuration;
import org.apache.hadoop.fs.Path;
import org.apache.hadoop.io.IntWritable;
import org.apache.hadoop.io.Text;
import org.apache.hadoop.mapreduce.Job;
import org.apache.hadoop.mapreduce.Mapper;
import org.apache.hadoop.mapreduce.Reducer;
import org.apache.hadoop.mapreduce.lib.input.FileInputFormat;
import org.apache.hadoop.mapreduce.lib.output.FileOutputFormat;

public class WordCount {

    public static class TokenizerMapper
            extends Mapper<Object, Text, Text, IntWritable> {

        private final static IntWritable one = new IntWritable(1);
        private Text word = new Text();

        public void map(Object key, Text value, Context context)
                throws IOException, InterruptedException {
            StringTokenizer itr = new StringTokenizer(value.toString());
            while (itr.hasMoreTokens()) {
                word.set(itr.nextToken());
                context.write(word, one);
            }
        }
    }

    public static class IntSumReducer
            extends Reducer<Text, IntWritable, Text, IntWritable> {
        private IntWritable result = new IntWritable();

        public void reduce(Text key, Iterable<IntWritable> values,
                           Context context) throws IOException, InterruptedException {
            int sum = 0;
            for (IntWritable val : values) {
                sum += val.get();
            }
            result.set(sum);
            context.write(key, result);
        }
    }

    public static void main(String[] args) throws Exception {
        Configuration conf = new Configuration();
        Job job = Job.getInstance(conf, "word count");
        job.setJarByClass(WordCount.class);
        job.setMapperClass(TokenizerMapper.class);
        job.setCombinerClass(IntSumReducer.class);
        job.setReducerClass(IntSumReducer.class);
        job.setOutputKeyClass(Text.class);
        job.setOutputValueClass(IntWritable.class);
        FileInputFormat.addInputPath(job, new Path(args[0]));
        FileOutputFormat.addOutputPath(job, new Path(args[1]));
        System.exit(job.waitForCompletion(true) ? 0 : 1);
    }
}

mkdir classes
javac -classpath $(/home/osboxes/hadoop/bin/hadoop classpath) -d classes WordCount.java

jar -cvf wordcount.jar -C classes/ .

/home/osboxes/hadoop/sbin/start-dfs.sh
/home/osboxes/hadoop/sbin/start-yarn.sh

hdfs dfs -mkdir /input
echo "Hadoop MapReduce Word Count Hadoop Example Example" > input.txt
hdfs dfs -put input.txt /input

hadoop jar wordcount.jar WordCount /input /output

hdfs dfs -rm -r /output

map 100% reduce 100%
Job Completed Successfully

hdfs dfs -cat /output/part-r-00000

/home/osboxes/hadoop/sbin/stop-yarn.sh
/home/osboxes/hadoop/sbin/stop-dfs.sh



Third party API
https://home.openweathermap.org/api_keys

App.js

import React, { useState } from "react";
import "./App.css";

const apiKey = "167aef2039f05117726bf058af6c7870"; 

function App() {
  const [city, setCity] = useState("");
  const [weather, setWeather] = useState(null);

  const getWeather = async () => {
    if (!city) return alert("Please enter a city");
    try {
      const res = await fetch(
        `https://api.openweathermap.org/data/2.5/weather?q=${city}&appid=${apiKey}&units=metric`
      );
      const data = await res.json();
      if (data.cod !== 200) {
        alert(data.message);
        return;
      }
      setWeather(data);
    } catch (err) {
      alert("Error fetching weather data");
    }
  };

  return (
    <div className="container">
      <h1>Weather App</h1>
      <input
        type="text"
        value={city}
        placeholder="Enter city"
        onChange={(e) => setCity(e.target.value)}
      />
      <button onClick={getWeather}>Get Weather</button>

      {weather && (
        <div className="box">
          <h2>{weather.name.toUpperCase()}</h2>
          <p>Temp: {weather.main.temp.toFixed(1)} °C</p>
          <p>Weather: {weather.weather[0].description.toUpperCase()}</p>
          <p>Humidity: {weather.main.humidity}%</p>
        </div>
      )}
    </div>
  );
}

export default App;
App.css
body {
  font-family: Arial, sans-serif;
  text-align: center;
  padding: 20px;
}

.container input,
.container button {
  padding: 8px;
  margin: 5px;
}

button {
  background: blue;
  color: white;
  border: none;
  border-radius: 5px;
  cursor: pointer;
}

.box {
  margin-top: 20px;
  padding: 15px;
  background: #e9f5ff;
  border-radius: 8px;
}
npx create-react-app weather-app
cd weather-app
npm start
